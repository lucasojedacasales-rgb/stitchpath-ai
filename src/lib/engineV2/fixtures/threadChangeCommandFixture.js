import { createGenericMascotCommandFixture } from './genericMascotCommandFixture.js';

export function createThreadChangeCommandFixture(config = {}) {
  return createGenericMascotCommandFixture(config);
}
